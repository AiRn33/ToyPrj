{{>layout/header}}
<body>
<br><br><br><br><br><br>

<div class="container text-center">
    <br>
    <div class="row">

        <div class="col-3"></div>
        <!-- Pills navs -->
        <div class="col-6">

            <ul class="nav nav-pills nav-justified mb-3" id="ex1" role="tablist">
                <li class="nav-item" role="presentation">
                    <a class="nav-link active" id="tab-login" data-mdb-toggle="pill" role="tab"
                       aria-controls="pills-login" aria-selected="true">MyPage</a>
                </li>
            </ul>
            <!-- Pills content -->
            <div class="tab-content">
                <div class="tab-pane fade show active" id="pills-login" role="tabpanel" aria-labelledby="tab-login">
                        <!-- Email input -->
                        <p class="text-center">
                            <button class="btn btn-primary me-2" disabled>아이디</button>
                        </p>

                        <div class="form-outline mb-4">
                            <input type="text" id="loginName" name="userId"class="form-control text-center" value="{{name.userId}}" disabled/>
                        </div>

                        <p class="text-center">
                            <button class="btn btn-primary me-2" disabled>이름</button>
                        </p>

                        <div class="form-outline mb-3">
                            <input type="text" name="userId"class="form-control text-center" value="{{name.userName}}" disabled/>
                        </div>


                        <!-- Submit button -->
                        <a class="btn btn-primary btn-block mb-4" onclick="location.href='/user/profileUpdate'">회원정보 수정</a>
                        <a class="btn btn-success btn-block mb-4" onclick="location.href='/user/profileDelete'">회원정보 삭제</a>
                        <a class="btn btn-secondary btn-block mb-4" onclick="location.href='/user/pwModify'">비밀번호 변경</a>
                        <a class="btn btn-danger btn-block mb-4" onclick="location.href='/main'">돌아가기</a>

                        <!-- Register buttons -->
                </div>
            </div>
            <div class="col-3"></div>
        </div>

    </div>




</body>
</html>